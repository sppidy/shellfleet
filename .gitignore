/target/
/sys-manager-agent_1.0.0_amd64/
*.deb
/agent-x86_64-linux
conductor/*
.claude/
design-handoff/
/LOG.md

# Operator secrets — JWT_SECRET, GITHUB_CLIENT_SECRET, AGENT_SECRET,
# OAuth IDs, etc. Use .env.example to share variable shapes; never
# commit a real .env.
.env
.env.local
.env.*.local
